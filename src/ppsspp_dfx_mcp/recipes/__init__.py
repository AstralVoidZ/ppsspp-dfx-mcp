"""MCP-builtin generic recipes (multi-step workflows).

Placeholder — no fully generic recipes exist yet (the archived
recipes hardcode project addresses and paths). Generic recipes added here
MUST obtain addresses via `config.addresses()` and MUST NOT reference
project paths.
"""
