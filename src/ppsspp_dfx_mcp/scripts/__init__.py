"""MCP-builtin generic scripts (PPSSPP-semantic only, no project-specific addresses).

Placeholder — no fully generic scripts exist yet (the archived
diagnostics hardcode project addresses). Generic scripts added here MUST
obtain addresses via `config.addresses()` and MUST NOT reference project
paths.
"""
